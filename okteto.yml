name: sigo-config
image: okteto/maven
command: ["mvn", "spring-boot:run" ]
workdir: /workspace/app
persistentVolume:
  enabled: false
forward:
- 8080:8080
resources:
  limits:
    cpu: "0.2"
    memory: 0.25Gi
